/**
 * NOTE:
 *
 * This declaration means that files that end in `.woff2`
 * and `.woff` don't throw errors when imported like modules
 * in TypeScript files.
 */

declare module "*.woff"
declare module "*.woff2"
