// @ts-check

/**
 * NOTE:
 * This exports the exactly the same `wrapPageElement`
 * definition as `gatsby-browser.js`, so re-exporting
 * that function means that changes don't have to be made
 * twice.
 */

export { wrapPageElement, wrapRootElement } from "./gatsby-browser"
